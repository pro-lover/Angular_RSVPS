import { DietActions, DietActionTypes } from './diet.actions';
import { featureAdapter, State } from "./diet.state";

const initialState = featureAdapter.getInitialState();

export function dietReducer(
	state = initialState,
	action: DietActions
): State {

	switch(action.type) {

		case DietActionTypes.COLLECTION_LOADED_SUCCESS:

			return featureAdapter.setAll(
				action.payload.collection,
				{...state,
					//collection: [],
					//selectedItem: null,
					isLoading: false,
					error: null,
					collectionLoaded:true
				}
			);

		case DietActionTypes.COLLECTION_CLEAR_ALL:

			return featureAdapter.removeAll(state);

		case DietActionTypes.MODEL_LoadedSuccess:

			//console.warn('action.type:', action.type);

			return featureAdapter.addOne(
				action.payload.dataItem,
				{
					...state,
					//collection: [],
					//selectedItem: null,
					isLoading: false,
					collectionLoaded:true,
					error: null
				  }
			);

		case DietActionTypes.MODEL_CreateSuccess:

			return featureAdapter.addOne(
				action.payload.dataItem,  {
					...state,
					collectionLoaded:true,
					isLoading: false,
					error: null
				  }
			);

		case DietActionTypes.MODEL_UpdateSuccess:

			return featureAdapter.updateOne(
				action.payload.dataItem,
				{
					...state,
					collectionLoaded:true,
					isLoading: false,
					error: null
				}
			);

		case DietActionTypes.MODEL_DeleteSuccess:

			return featureAdapter.removeOne(
				action.payload.dataId,
				{
					...state,
					allPositionsLoaded: true,
					isLoading: false,
					error: null
				}
			);

		case DietActionTypes.MODEL_LoadedFailed:
		case DietActionTypes.MODEL_UpdateFailed:
		case DietActionTypes.MODEL_UpdateStatusFailed:
		case DietActionTypes.MODEL_DeleteFailed:
		case DietActionTypes.MODEL_RestoreFailed:
		case DietActionTypes.MODEL_CreateFailed:
		case DietActionTypes.COLLECTION_LOADED_FAILED:

			console.error('FAILED REDUCER:', action.type, featureAdapter);

			return state;

		default: {

			//console.warn('DEFAULT REDUCER:', action.type, featureAdapter);

			return state;

			return {
				...state,
				//collection: [],
				//selectedItem: null,
				collectionLoaded:true,
				isLoading: false,
				error: null
			};
		}

	}
}

export const {
selectAll,
selectEntities,
selectIds,
selectTotal

} = featureAdapter.getSelectors();
