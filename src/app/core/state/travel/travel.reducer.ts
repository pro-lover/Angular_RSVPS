import { TravelActions, TravelActionTypes } from './travel.actions';
import { featureAdapter, State } from "./travel.state";

const initialState = featureAdapter.getInitialState();

export function travelReducer(
	state = initialState,
	action: TravelActions
): State {

	switch(action.type) {

		case TravelActionTypes.COLLECTION_LOADED_SUCCESS:

			return featureAdapter.setAll(
				action.payload.collection,
				{...state,
					//collection: [],
					//selectedItem: null,
					isLoading: false,
					error: null,
					collectionLoaded:true
				}
			);

		case TravelActionTypes.COLLECTION_CLEAR_ALL:

			return featureAdapter.removeAll(state);

		case TravelActionTypes.MODEL_LoadedSuccess:

			//console.warn('action.type:', action.type);

			return featureAdapter.addOne(
				action.payload.dataItem,
				{
					...state,
					//collection: [],
					//selectedItem: null,
					isLoading: false,
					collectionLoaded:true,
					error: null
				  }
			);

		case TravelActionTypes.MODEL_CreateSuccess:

			return featureAdapter.addOne(
				action.payload.dataItem,  {
					...state,
					collectionLoaded:true,
					isLoading: false,
					error: null
				  }
			);

		case TravelActionTypes.MODEL_UpdateSuccess:

			return featureAdapter.updateOne(
				action.payload.dataItem,
				{
					...state,
					collectionLoaded:true,
					isLoading: false,
					error: null
				}
			);

		case TravelActionTypes.MODEL_DeleteSuccess:

			return featureAdapter.removeOne(
				action.payload.dataId,
				{
					...state,
					allPositionsLoaded: true,
					isLoading: false,
					error: null
				}
			);

		case TravelActionTypes.MODEL_LoadedFailed:
		case TravelActionTypes.MODEL_UpdateFailed:
		case TravelActionTypes.MODEL_UpdateStatusFailed:
		case TravelActionTypes.MODEL_DeleteFailed:
		case TravelActionTypes.MODEL_RestoreFailed:
		case TravelActionTypes.MODEL_CreateFailed:
		case TravelActionTypes.COLLECTION_LOADED_FAILED:

			console.error('FAILED REDUCER:', action.type, featureAdapter);

			return state;

		default: {

			//console.warn('DEFAULT REDUCER:', action.type, featureAdapter);

			return state;

			return {
				...state,
				//collection: [],
				//selectedItem: null,
				collectionLoaded:true,
				isLoading: false,
				error: null
			};
		}

	}
}

export const {
selectAll,
selectEntities,
selectIds,
selectTotal

} = featureAdapter.getSelectors();
